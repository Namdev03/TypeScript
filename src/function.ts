function makeChai(type: string, cupe: number) {
    console.log(`make chai${type}cup of ${cupe}`);

}
makeChai("masalachai", 2)

function getChai(): number {
    return 34
}
getChai()
function makeorder(order: string): string {
    if (!order) return "ewre"
    return order
}
type chai = {
    name: string;
    price: number
}
const menu: chai[] = [{
    name: "masala", price: 25
},
{ name: "masala", price: 25 }
]
